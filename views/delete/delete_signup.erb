<% if @signup.signup_id %>
  <div class="panel panel-danger">
    <div class="panel-heading">
        <h3 class="panel-title">Eliminar inscripción</h3>
    </div>
    <div class="panel-body">
        <p>¿Está usted seguro de <b>eliminar</b> esta inscripción? Esta acción no puede ser revertida.</p>
        <ol style="list-style-type:disc;">
        <% Student.where(student_id: @signup.student_id).select(:student_name).each do |student| %>
        <li><b>Estudiante</b>: <%=h student.student_name %></li>
        <% end %>
        <% User.where(user_id: @signup.user_id).select(:user_name).each do |user| %>
        <li><b>Emisor</b>: <%=h user.user_name %></li>
        <% end %>
        <li><b>Monto cancelado</b>: <%= @signup.signup_amount %> BsF.</li>
        <li><b>Método de pago</b>: <%= @signup.payment_type %></li>
        <li><b>Fecha de emision</b>: <%= @signup.issue_date.strftime('%d/%m/%Y') %></li>
        <li><b>Fecha de expiración</b>: <%= @signup.expiration_date.strftime('%d/%m/%Y') %></li>
        <% unless @signup.bank.empty? %>
        <li><b>Banco</b>: <%=h @signup.bank %></li>
        <% end %>
        <% unless @signup.payment_type == 'Efectivo' %>
        <li><b>Número de referencia</b>: <%=h @signup.reference_number %></li>
        <% end %>
        <li><b>Concepto</b>: <%= h(@signup.signup_description) %></li>
        <% unless @signup.signup_notes.blank? %>
        <li><b>Notas</b>: <%= h(@signup.signup_notes) %></li>
        <% end %>
        </ol>
        <form action="/dashboard/students/<%= @signup.student_id %>/signups/<%= @signup.signup_id %>/delete" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-danger">Eliminar</button>
          <a href="/dashboard/students/<%= @signup.student_id %>/signups" class="btn btn-default">Cancelar</a>
        </form>
    </div>
  </div>
<% end %>
