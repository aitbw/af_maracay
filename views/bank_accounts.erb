<% if flash[:notice] %>
  <p class="success"><%= flash[:notice] %></p>
<% end %>
<% if flash[:error] %>
  <p class="error"><%= flash[:error] %></p>
<% end %>
<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col">Banco</th>
      <th scope="col">Número de cuenta</th>
      <th scope="col">Tipo de cuenta</th>
      <th scope="col">Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @accounts.each do |account| %>
    <% Banco.where(idBanco: account.idBanco).each do |bank| %>
      <tr>
        <td><%= bank.nombreBanco %></td>
        <td><%=h account.numeroCuenta %></td>
        <td><%= account.tipoCuenta %></td>
        <td>
          <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Acciones<span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li><a href="/dashboard/teachers/<%= @id_profesor %>/bank_accounts/<%= account.idCuentaBanco %>/delete">Eliminar</a></li>
              <li><a href="/dashboard/teachers/<%= @id_profesor %>/bank_accounts/<%= account.idCuentaBanco %>/edit">Modificar</a></li>
            </ul>
          </div>
        </td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<a href="/dashboard/teachers/<%= @id_profesor %>/bank_accounts/add" class="btn btn-success">Asignar cuenta bancaria</a>
<a href="/dashboard/teachers" class="btn btn-default">Atrás</a>
